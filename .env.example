# ctxzip — env template. License: BUSL-1.1 (see LICENSE in repo; not MIT).
# Copy to .env in this directory (never commit .env)
# Semantic search (recommended)
OPENAI_API_KEY=

# Optional: AI-generated docstrings at index time
ANTHROPIC_API_KEY=

